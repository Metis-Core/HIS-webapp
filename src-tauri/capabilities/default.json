{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Metis Healthcare desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
